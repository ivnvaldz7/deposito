DATABASE_URL="postgresql://deposito:deposito123@localhost:5432/deposito"
# JWT_SECRET debe ser un string largo y aleatorio (mínimo 32 caracteres).
# Generá uno con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# El servidor no arranca si esta variable no está configurada.
JWT_SECRET=""
PORT=3001
CLIENT_URL="http://localhost:5173"
