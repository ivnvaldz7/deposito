{
  "name": "platform",
  "private": true,
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
